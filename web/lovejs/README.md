# love.js Player Files

Populate this directory via:

```bash
make fetch-lovejs
```

Expected result: this folder contains `player.js`, `lua/normalize*.lua`, and `11.5/release/*` runtime files required by love.js.

Project: https://github.com/2dengine/love.js
